"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StartupTaskProvider = exports.StartupTask = void 0;
var StartupTask;
(function (StartupTask) {
    function is(arg) {
        if (typeof arg === 'object') {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const object = arg;
            return ('command' in object &&
                typeof object['command'] === 'string' &&
                (!('args' in object) || Array.isArray(object['args'])));
        }
        return false;
    }
    StartupTask.is = is;
    function has(arg) {
        if (typeof arg === 'object') {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const object = arg;
            return ('tasks' in object &&
                Array.isArray(object['tasks']) &&
                object['tasks'].every(is));
        }
        return false;
    }
    StartupTask.has = has;
    let Messaging;
    (function (Messaging) {
        Messaging.STARTUP_TASKS_SIGNAL = 'arduino/startupTasks';
        function APP_READY_SIGNAL(id) {
            return `arduino/appReady${id}`;
        }
        Messaging.APP_READY_SIGNAL = APP_READY_SIGNAL;
    })(Messaging = StartupTask.Messaging || (StartupTask.Messaging = {}));
})(StartupTask = exports.StartupTask || (exports.StartupTask = {}));
exports.StartupTaskProvider = Symbol('StartupTaskProvider');
//# sourceMappingURL=startup-task.js.map